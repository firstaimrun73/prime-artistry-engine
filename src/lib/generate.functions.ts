/**
 * generateMedia server fn — thin entry.
 * Handler body lives in generate-media-handler (keeps entry small; full logic restored).
 */
export { generateMedia, completeCheckout } from "./generate-media-handler";
