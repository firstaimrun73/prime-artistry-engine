/**
 * Circle 2edit — /studio/image/circle-remove
 * Remove: circleInstant true
 * Add: circleInstant false → flux-pro fill; asset rail + factors + confirm
 * Exit/Back respects from=home|info|sample|studio.
 * Auth: logged-out users redirected to /auth.
 * hasImage collapses header; floating toolbar from CircleEditShell.
 */
export { Route } from "./studio.image.circle-remove.impl";
