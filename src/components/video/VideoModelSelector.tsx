/**
 * DEPRECATED — model selection is backend-only.
 * This component must not be rendered in Video Studio.
 * Kept as a no-op export so accidental imports do not show model cards.
 */
export function VideoModelSelector(_props: Record<string, unknown>) {
  return null;
}
