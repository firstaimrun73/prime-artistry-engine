/**
 * Image Editor workspace — independent of Video Editor.
 * UPLOAD → PROMPT → SELECT → GENERATE → OUTPUT
 * Phase 2: Stop/Cancel removed (hideStop).
 */
export { ImageEditor } from "./ImageEditor.impl";
