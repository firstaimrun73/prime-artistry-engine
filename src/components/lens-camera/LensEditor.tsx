/**
 * Motio2edit Lenses — Snapchat-style full-screen camera UI.
 * RESTORE_PENDING — temporary stub; full file follows in next commit
 */
export function LensEditor(_props: { initialLensId?: string }) {
  return null;
}
export default LensEditor;
