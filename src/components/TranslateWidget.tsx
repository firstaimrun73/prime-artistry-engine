// Native locale only — Google Translate is intentionally NOT used.
// Language is selected in Settings and applied via src/lib/i18n (setLocale).
// This component is kept as a no-op mount point so root layout stays stable.

export function TranslateWidget() {
  return null;
}
