/**
 * EXTRAA retired — wide/cinematic media lives in the unified Discover feed.
 * Kept as a no-op export so any residual imports do not break the build.
 */
export function ExtraaSection() {
  return null;
}
