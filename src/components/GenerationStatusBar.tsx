/**
 * Legacy floating "running bar" — retired.
 * Image / Video / Music studios use dedicated generation screens/overlays
 * while the AI request is processing. This component intentionally renders nothing.
 */
export function GenerationStatusBar() {
  return null;
}
